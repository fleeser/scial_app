class: AuthForgotPasswordRequest

table: auth_forgot_password_request

fields:
  email: String
  verificationCode: String

indexes:
  auth_forgot_password_request_email_idx:
    fields: email
  auth_forgot_password_request_verification_code_idx:
    fields: verificationCode