name: scial_app_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  serverpod: ^1.0.1

  scial_app_shared:
    path: ../scial_app_shared

  crypto: ^3.0.2
  sendgrid_mailer: ^0.1.3

dev_dependencies:
  lints: ^2.0.0
